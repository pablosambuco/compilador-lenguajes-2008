
TYPE frutas AS ["manzana","naranja"];
TYPE valores AS [12.3, 14.2];
DEFVAR
	jose: FLOAT; #% Todos los numericos son FLOAT %#
	mivariable, otraVariable, estavariable: POINTER;
	tipoExtra: frutas;
	otraMas: valores;
ENDDEF
BEGIN 
	#%IF(!(carlos + marta / pedro <= raul - ariel))
		z = a + 3 * 2;
		z = x * 3 + y / 2 - 5;
	ELSE	
		raul = mario / (45.89 + jennifer);
	ENDIF
	IF(carlos < ariel)
		a = b + c;
	ELSE
		Raiz = 1 + 3;
	ENDIF	%#	
END