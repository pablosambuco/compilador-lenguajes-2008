
#% 
 % Este es un programa de prueba.
 % La idea de este programa es mostrar todas las palabras reservadas y alguno simbolos
 % admitidos por el lenguaje.
 % No deberia contener ningun error lexico.
 %#
 
TYPE frutas AS ["manzana","naranja","cualquiera..."];
TYPE valores AS [12.3, 14.2, 55, .64];
DEFVAR
	jose, esteban: FLOAT; #% Todos los numericos son FLOAT %#
	roberto, julian: STRING; #% El tamaño maximo para las cadenas es 30 caracteres %#
	otraVariable: POINTER;
	tipoExtra: frutas;
	otraMas: valores;
ENDDEF
BEGIN 
	jose = 5;
	IF (jose != 2 && jose > 4) 
		jose = 26 + 9 / 8 * (7 + 2) / vagancia * 3 + AVG([234,443.1,22,768]) + 98;
		IF(a != b || juanSancho <= 3 )
			DISPLAY("Mmm, IFs anidados");
		ELSE
			REPEAT
				DISPLAY("ya es muy groso");
			UNTIL(!SuHijo < 2); #% Ojo con esto, el uso de ! queda horrible asi, ya que pareciera que esta negando a SuHijo (?) y en verdad está negando la comparacion %#
		ENDIF
			
	ELSE
		DISPLAY("te quieroooo");
	ENDIF
	
	REPEAT
		DISPLAY("te odiooooo");
		jose = jose - 1;
	UNTIL(jose < 2);

END  #% Holaaaaa #%anidamieeeento%# Chauuuuu %#
				