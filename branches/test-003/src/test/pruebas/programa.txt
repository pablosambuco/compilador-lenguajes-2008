
TYPE frutas AS ["manzana","naranja"];
TYPE valores AS [12.3, 14.2];
DEFVAR
	jose, mario, raul, ernesto: FLOAT; #% Todos los numericos son FLOAT %#
	mivariable, otraVariable, estavariable, carlos: POINTER;
	tipoExtra, a, b: frutas;
	otraMas: valores;
	falla: POINTER;
ENDDEF
BEGIN 
	#%IF(!(carlos + marta / pedro <= raul - ariel))
		z = a + 3 * 2;
		z = x * 3 + y / 2 - 5;
	ELSE	
		raul = mario / (45.89 + jennifer);
	ENDIF%#
	IF(jose < mario)
		raul = ernesto + b;
	ELSE
		jose = 1 + 3;
	ENDIF	
END