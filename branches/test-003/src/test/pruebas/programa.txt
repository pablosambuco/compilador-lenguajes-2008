#%TYPE frutas AS ["manzana","naranja","cualquiera"];
TYPE valores AS [12.3, 14.2, 55, .64];
DEFVAR
	jose, esteban, raul: FLOAT;
	roberto, julian: STRING;
	otraVariable: POINTER;
	tipoExtra: frutas;
	otraMas: valores;
ENDDEF%#
BEGIN 
	#%jose = 25 * esteban + raul;
	roberto = "HOLA CHICHEEE";
	tipoExtra = "manzana";
	otraMas = 12.3;%#
	
	probando = 3;
	sigo = viendo + si + anda;
	
	IF(cuatro != 5)	
		yo = soy + pablo;
		otra = sentencia;
	ENDIF
	
	IF(te <= aburris && no == entendes)	
		lo = siento;
		mucho = mucho;
	ELSE
		me = alegro;
		un = monton;	
	ENDIF
	
	rellenamos = un - poco;
	
	REPEAT
		seguimos = un + poco + mas;
		ya = estamos;
	UNTIL(the / end + of >= the * world);
	
	
END